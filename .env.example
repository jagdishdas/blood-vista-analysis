# =====================================================
# VERCEL DEPLOYMENT CONFIGURATION
# =====================================================
# Set these in Vercel Dashboard -> Settings -> Environment Variables

# OpenAI API Key (REQUIRED for chatbot and radiology features)
OPENAI_API_KEY=sk-your-openai-api-key

# =====================================================
# FRONTEND CONFIGURATION (Optional)
# =====================================================

# Custom API URL (optional - auto-detects Vercel deployment)
# VITE_API_URL=https://your-app.vercel.app

# AI Provider Selection (optional - defaults to openai)
# Options: openai, gemini, claude
# VITE_AI_PROVIDER=openai

# =====================================================
# SUPABASE CONFIGURATION (for database features)
# =====================================================
# These are auto-configured when using Lovable Cloud
# For manual setup, get these from Supabase Dashboard -> Settings -> API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# =====================================================
# SUPABASE EDGE FUNCTION SECRETS (Fallback)
# Only needed if NOT using Vercel serverless functions
# Set these in Supabase Dashboard -> Edge Functions -> Secrets
# =====================================================

# Google Cloud Vision API - Service Account Credentials (for OCR)
# GOOGLE_CLOUD_PROJECT_ID=your-google-cloud-project-id
# GOOGLE_CLOUD_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# GOOGLE_CLOUD_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Alternative AI providers (optional)
# GEMINI_API_KEY=your-gemini-api-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
